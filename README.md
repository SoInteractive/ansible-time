Ansible role to set proper NTP servers
======================================

Role checks if ntp or chrony is available and configures one to access
NTP servers served by comarch, hetzner or europe pool based on autodetection.
